Host firmware regression tests are in host/.

Run from the firmware directory on a Bash/GCC system:

    bash test/run_host_tests.sh

This compiles production source against API mocks with AddressSanitizer and
UndefinedBehaviorSanitizer. See ../../Docs/Testing.md for coverage, exclusions,
the real Teensy build commands and required hardware measurements.
